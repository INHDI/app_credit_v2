services:
  scheduler:
    build:
      context: ./scheduler
      dockerfile: Dockerfile
    container_name: app_credit_scheduler_v3
    restart: unless-stopped
    environment:
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ./scheduler/logs:/var/log
    env_file:
      - ./scheduler/.env
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
