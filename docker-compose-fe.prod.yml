services:
  frontend:
    image: ${DOCKER_IMAGE_FRONTEND:-app-credit-frontend}:${TAG:-latest}
    restart: unless-stopped
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - BUILDKIT_INLINE_CACHE=1
    environment:
      - NODE_ENV=production
      - TZ=Asia/Ho_Chi_Minh
      # Expose your API base URLs to the app if needed
      # - NEXT_PUBLIC_API_BASE_URL=http://10.15.242.51:8089
      # - NEXT_PUBLIC_WS_URL=ws://10.15.242.51:8089
    ports:
      - "3004:3000"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
